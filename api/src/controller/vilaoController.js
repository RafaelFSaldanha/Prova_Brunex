import { Router } from 'express'
import { vilao } from '../repository/vilaoRepository'

const server = Router();

server.get('/vilao/:id', async (req, resp) => {
    try{ 
        const { id } 
    }
})